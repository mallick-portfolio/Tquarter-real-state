const sectionTitle = () => {
  return (
    <div className="text-center py-12">
      <h2 className="text-primary text-4xl font-bold mb-4">Our listings portfolio</h2>
      <p>These are the latest properties in the Sales category. You can create the list using <br /> the “latest listing shortcode” and show items by specific categories.</p>
    </div>
  )
}

export default sectionTitle